@model WebCRUDMVCSQL.Models.USUARIOS

<style>
      .bd-placeholder-img {
        font-size: 1.125rem;
        text-anchor: middle;
        -webkit-user-select: none;
        -moz-user-select: none;
        user-select: none;
      }

      @@media (min-width: 768px) {
        .bd-placeholder-img-lg {
          font-size: 3.5rem;
        }
      }
    </style>

    
    <!-- Custom styles for this template -->
    <link href="signin.css" rel="stylesheet">
 
    
<main class="form-signin">
    <form action="/Login/ProcessarLogin" method="post">
      
    @using (Html.BeginForm("ProcessarLogin", "Login"))
        {

    <img class="mb-4" src="./image/criar-site.png" alt="" width="300" height="300">
    <h1 class="h3 mb-3 fw-normal">Login </h1>
       @Html.AntiForgeryToken()
       //Estas linhas comentadas são outra forma de enviar tipadas pela model, funciona igual, mas implementar o botrap 5 fica mais complicado.
        @*<div class="form-group">
            @Html.LabelFor(m => m.NOME_USUARIO)
            @Html.TextBoxFor(m => m.NOME_USUARIO, "", new { @class = "form-control", @placeholder = "Usuário" })

        </div>
        <div class="form-floating" >
            @Html.LabelFor(m => m.Senha)
            @Html.PasswordFor(m => m.Senha, new { @class = "form-control" , id="floatingInput", @placeholder = "Senha" })
        </div>
        <div class="checkbox mb-3">
            <label>
                <input type="checkbox" value="remember-me"> Me Lembre
            </label>
        </div>
        <p>
            @Html.ActionLink("Esqueceu a Senha ?", "ForgotPassword")
    </p>

        <div class="form-group form-floating" >
            <input type="submit" name="submit" class="btn btn-primary" value="Usuário" />
    </div>*@
        <div class="form-group form-floating">
                <input type="text" class="form-control" name="NOME_USUARIO"  id="floatingInput" placeholder="Usuário">
            <label for="nome_usuario" name="NOME_USUARIO">Nome de Usuário</label>
    </div>
            <br />
        <div class="form-group form-floating">
                <input type="password" class="form-control" name="Senha" id="floatingPassword" placeholder="Senha">
            <label for="Senha" name="Senha" >Senha</label>
    </div>

    <div class="checkbox mb-3">
      <label>
        <input type="checkbox" value="remember-me"> Me Lembre
      </label>
    </div>
    <button class="w-100 btn btn-lg btn-primary" type="submit">Entrar</button>


        <p>
            @Html.ActionLink("Esqueceu a Senha ?", "ForgotPassword")
        </p>
    <p class="mt-5 mb-3 text-muted">&copy; 2017–2021</p>
        }
  </form>
</main>
<script> </script>
 